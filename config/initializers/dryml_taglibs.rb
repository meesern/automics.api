#Comment out this line to fix the theme not updating properly bug (as seen by
#clean theme with ajax spinners and search sidebar
#Dryml.precompile_taglibs if File.basename($0) != "rake" && Rails.env.production?
